/* GLOBAL STYLES */
body {
    font-family: 'Segoe UI', Roboto, sans-serif;
    margin: 0;
    background-color: #f4f7f6;
    color: #333;
}

/* HEADER & LOGO */
header {
    background: #121921;
    color: white;
    padding: 40px 0;
    text-align: center;
    border-bottom: 3px solid #e74c3c;
}

/* WOLF LOGO DESIGNED IN CSS */
.wolf-logo {
    width: 80px;
    height: 80px;
    background-color: #e74c3c;
    margin: 0 auto 15px;
    /* This creates a sharp, geometric wolf-like shape using clip-path */
    clip-path: polygon(50% 0%, 80% 30%, 100% 70%, 80% 90%, 50% 70%, 20% 90%, 0% 70%, 20% 30%);
    transition: transform 0.3s ease;
}

.wolf-logo:hover {
    transform: scale(1.1) rotate(5deg);
    background-color: #ff4d4d;
}

/* CONTAINERS */
.container {
    width: 90%;
    max-width: 1000px;
    margin: 0 auto;
    padding: 20px 0;
}

.hero {
    background: white;
    padding: 40px;
    border-radius: 10px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    text-align: center;
}

.section {
    display: flex;
    gap: 20px;
    margin-top: 30px;
}

.card {
    flex: 1;
    background: white;
    padding: 25px;
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.05);
}

/* INPUTS */
textarea {
    width: 100%;
    height: 180px;
    border: 1px solid #ddd;
    border-radius: 5px;
    padding: 10px;
    font-family: inherit;
    resize: none;
    box-sizing: border-box;
}

textarea:focus {
    outline: 2px solid #e74c3c;
}

/* BUTTONS */
.btn {
    background: #e74c3c;
    color: white;
    padding: 12px 30px;
    border: none;
    border-radius: 25px;
    font-size: 1rem;
    font-weight: bold;
    cursor: pointer;
    transition: 0.2s;
}

.btn:hover {
    background: #c0392b;
    box-shadow: 0 5px 15px rgba(231, 76, 60, 0.4);
}

/* FOOTER */
footer {
    margin-top: 50px;
    padding: 40px 0;
    background: #121921;
    color: #888;
    text-align: center;
}

/* RESPONSIVE */
@media (max-width: 768px) {
    .section {
        flex-direction: column;
    }
}